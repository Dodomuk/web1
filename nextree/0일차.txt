스프링의 경우 
톰켓에 모든 설정들을 기본적으로 해줘야 한다. (web.xml)

스프링부트의 경우
-기본적으로 톰켓이 설정되엉 있음(web.xml설정을 따로 하지 않는다)
-그래서 포트 번호를 바꾸는 등의 작업은 어플리케이션 쪽에서 텍스트 혹은 마크업 형태로 지정한다.

url: 자원의 위치
uri:자원과 자원의 위치
rest의 구조 :
자원 
행위
표현

spring boot에서 의존성을 주입하는 3가지 방법

1. Autowired 어노테이션 사용

2. 주입할 필드 관련 생성자를 만든다.
public UserServiceLogic(UserStore userStore){
    this.userStore = userStore;
}

3. @RequiredArgsConstructor: 주입할 필드를 final로 지정하면 초기화시 값이 변하지 않는 특성을 갖기 때문에 이 어노테이션을 통해 생성자가 추가로 생성이된다.
'

*****단위 테스트를 진행할 경우 의존성 주입은 @Autowired 방식만 가능하다!

[ @RequestBody란? ]
@RequestBody는 클라이언트가 전송하는 Json(application/json) 형태의 HTTP Body 내용을 Java Object로 변환시켜주는 역할을 한다. 그렇기 때문에 Body가 존재하지 않는 Get 메소드에 @RequestBody를 활용하려고 한다면 에러가 발생하게 된다. @RequestBody로 받는 데이터는 Spring에서 관리하는 MessageConverter들 중 하나인 MappingJackson2HttpMessageConverte를 통해 Java 객체로 변환된다.


